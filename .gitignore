# Dependencies
/node_modules

# Editor configuration
.vscode/

# Ignore the entire build output directory first
# 忽略整个css目录
/css/

# BUT, do NOT ignore the source file inside it!
# 但是，不要忽略目录里的这个源文件！
!/css/input.css

# Also, explicitly ignore the build artifact, just in case.
# 为保险起见，再次明确地忽略构建产物。
/css/style.css

# Other common ignores
/dist/
.env
.env.local
.env.*.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*
